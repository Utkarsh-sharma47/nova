# Extractor evaluation fixtures

Labeled cases for the eight GoComet assignment fields.

Run:

```bash
PYTHONPATH=src python -c "from nova.evaluation.extractor import run_extractor_evaluation; print(run_extractor_evaluation()[0])"
```

Or via `scripts/run_full_evaluation.py` (includes extractor suite).
