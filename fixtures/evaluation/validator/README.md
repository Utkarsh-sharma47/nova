# Validator evaluation fixtures

Synthetic cases only — no customer PII.

| Path | Purpose |
|------|---------|
| `cases/` | Full eval catalog |
| `regression/` | Fixed regression subset (safety-critical) |

Run: `python scripts/run_validator_eval.py`
