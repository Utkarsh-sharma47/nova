# Document fixtures

Synthetic builders live in `tests/documents/fixtures.py`.

This directory is reserved for optional checked-in **non-sensitive** sample bytes.
Do not store real customer shipping documents or PII here.
